# 国心解读微信客服后端修改建议

## 1. 修改背景

当前前端通过以下接口获取微信客服链接：

```http
GET /api/yiqixue/app/guoxin/kfLink
```

当前返回的链接类似：

```text
https://work.weixin.qq.com/kfid/kfce349d33f49eedcbb
```

实际使用时存在以下现象：

1. 用户第一次从 App 跳转微信客服时，可以正常进入客服会话。
2. 第二次从 App 唤醒微信时，可能只停留在微信联系人或聊天列表，不能自动进入客服聊天详情。
3. 客服结束会话、用户删除会话后，再次打开相同链接仍可能只停留在联系人列表。

当前链接疑似为固定的裸 `kfid` 地址，没有通过企业微信官方接口按业务场景生成完整客服链接。建议后端改为调用企业微信官方“获取客服帐号链接”接口，并把企业微信返回的完整 URL 原样返回给前端。

> 说明：使用官方完整链接可以保证场景参数合法，并正常参与进入会话事件校验；但微信最终是否每次直接打开聊天详情，仍由微信客户端根据会话状态决定，H5 无法强制控制。

## 2. 改造目标

后端需要完成以下改造：

1. 使用企业微信客服应用的 `CorpID` 和 `Secret` 获取 `access_token`。
2. 使用客服帐号的 `open_kfid` 调用 `/cgi-bin/kf/add_contact_way`。
3. 根据业务入口传入自定义 `scene`。
4. 将企业微信返回的完整 `url` 原样返回给前端，不删除、替换或重新拼接 `enc_scene`。
5. 缓存 `access_token` 和不同场景对应的客服链接。
6. 对前端传入的场景值进行白名单校验。

## 3. 正式调用链路

```text
国心解读 H5
  ↓
GET /api/yiqixue/app/guoxin/kfLink?scene=guoxin_buy
  ↓
国心解读后端校验 scene 白名单
  ↓
读取缓存中的企业微信 access_token
  ↓（缓存不存在或即将过期）
调用企业微信 gettoken 接口
  ↓
调用企业微信 /cgi-bin/kf/add_contact_way
  ↓
企业微信返回带 enc_scene 的完整客服 URL
  ↓
国心解读后端将完整 URL 返回给 H5
  ↓
H5 使用 window.location.href 跳转完整 URL
```

## 4. 后端配置

建议增加以下配置：

```yaml
wecom:
  corp-id: wwxxxxxxxxxxxxxxxx
  kf-secret: xxxxxxxxxxxxxxxxxxxxxxxx
  open-kfid: wkxxxxxxxxxxxxxxxx
```

参数说明：

- `corp-id`：企业微信企业 ID。
- `kf-secret`：具有“微信客服 → 获取基础信息”权限的 Secret。
- `open-kfid`：目标微信客服帐号 ID。

安全要求：

- `kf-secret` 必须通过环境变量、配置中心或密钥管理服务保存。
- 禁止把 `kf-secret`、`access_token` 返回给前端。
- 禁止在普通业务日志中打印完整 Secret 或 access_token。

## 5. 获取企业微信 access_token

请求：

```http
GET https://qyapi.weixin.qq.com/cgi-bin/gettoken
    ?corpid=CORP_ID
    &corpsecret=KF_SECRET
```

成功响应示例：

```json
{
  "errcode": 0,
  "errmsg": "ok",
  "access_token": "ACCESS_TOKEN",
  "expires_in": 7200
}
```

建议：

- 按企业维度缓存 access_token。
- 建议缓存约 7000 秒，不要等到 7200 秒后再刷新。
- 并发刷新时应加分布式锁或单机锁，避免短时间重复申请 token。
- 企业微信返回非零 `errcode` 时，不要继续调用客服链接接口。

## 6. 获取客服帐号链接

请求：

```http
POST https://qyapi.weixin.qq.com/cgi-bin/kf/add_contact_way
     ?access_token=ACCESS_TOKEN
Content-Type: application/json
```

请求体：

```json
{
  "open_kfid": "wkxxxxxxxxxxxxxxxx",
  "scene": "guoxin_buy"
}
```

成功响应示例：

```json
{
  "errcode": 0,
  "errmsg": "ok",
  "url": "https://work.weixin.qq.com/kf/xxxx?enc_scene=ENCGxxxxxxxx"
}
```

后端必须使用响应中的完整 `url`，不得执行以下操作：

- 不得删除 `enc_scene`。
- 不得自行生成 `enc_scene`。
- 不得把一个链接的 `enc_scene` 复制到另一个链接。
- 不得只截取 `/kf/xxxx` 或 `/kfid/xxxx` 部分。
- 不得将企业微信返回的参数重新排序、解密或修改。

## 7. scene 设计建议

`scene` 是由开发者自定义的业务来源，不是企业微信固定枚举值。

官方约束：

- 最多 32 字节。
- 只允许数字、大小写字母、下划线和短横线。
- 字符范围：`[0-9a-zA-Z_-]*`。

建议本项目使用以下场景：

| scene | 业务含义 |
| --- | --- |
| `guoxin_default` | 默认客服入口 |
| `guoxin_free` | 首页“限时免费”入口 |
| `guoxin_buy` | 购买套餐入口 |
| `guoxin_quota` | 问答次数不足入口 |
| `guoxin_report` | 报告次数不足入口 |
| `guoxin_credits` | 权益购买页客服入口 |
| `guoxin_member` | 会员权益页客服入口 |

后端必须使用白名单，不允许前端传入任意 scene：

```java
private static final Set<String> ALLOWED_SCENES = Set.of(
    "guoxin_default",
    "guoxin_free",
    "guoxin_buy",
    "guoxin_quota",
    "guoxin_report",
    "guoxin_credits",
    "guoxin_member"
);
```

非法或缺失的场景值统一回退为：

```text
guoxin_default
```

## 8. kfLink 接口建议

建议保留当前接口地址，并增加可选的 `scene` 参数：

```http
GET /api/yiqixue/app/guoxin/kfLink?scene=guoxin_buy
```

成功响应：

```json
{
  "code": 200,
  "msg": "请求成功",
  "success": null,
  "data": {
    "url": "https://work.weixin.qq.com/kf/xxxx?enc_scene=ENCGxxxxxxxx"
  }
}
```

失败响应示例：

```json
{
  "code": 500,
  "msg": "暂时无法获取客服链接，请稍后再试",
  "success": false,
  "data": null
}
```

不要把企业微信返回的 access_token、Secret 或完整内部错误堆栈返回给前端。

## 9. Java Spring Boot 示例

### 9.1 Controller

```java
@RestController
@RequestMapping("/api/yiqixue/app/guoxin")
public class GuoxinCustomerServiceController {

    private final WecomCustomerService wecomCustomerService;

    public GuoxinCustomerServiceController(
            WecomCustomerService wecomCustomerService) {
        this.wecomCustomerService = wecomCustomerService;
    }

    @GetMapping("/kfLink")
    public Result<Map<String, String>> getKfLink(
            @RequestParam(required = false) String scene) {

        String url = wecomCustomerService.getContactUrl(scene);
        return Result.success(Map.of("url", url));
    }
}
```

### 9.2 Service

```java
@Service
public class WecomCustomerService {

    private static final Set<String> ALLOWED_SCENES = Set.of(
        "guoxin_default",
        "guoxin_free",
        "guoxin_buy",
        "guoxin_quota",
        "guoxin_report",
        "guoxin_credits",
        "guoxin_member"
    );

    private final RestTemplate restTemplate;
    private final WecomProperties properties;
    private final WecomAccessTokenService accessTokenService;
    private final CustomerLinkCache customerLinkCache;

    public WecomCustomerService(
            RestTemplate restTemplate,
            WecomProperties properties,
            WecomAccessTokenService accessTokenService,
            CustomerLinkCache customerLinkCache) {
        this.restTemplate = restTemplate;
        this.properties = properties;
        this.accessTokenService = accessTokenService;
        this.customerLinkCache = customerLinkCache;
    }

    public String getContactUrl(String requestedScene) {
        String scene = normalizeScene(requestedScene);
        String cacheKey = properties.getOpenKfid() + ":" + scene;

        String cachedUrl = customerLinkCache.get(cacheKey);
        if (cachedUrl != null && !cachedUrl.isBlank()) {
            return cachedUrl;
        }

        String accessToken = accessTokenService.getAccessToken();
        String api = "https://qyapi.weixin.qq.com/cgi-bin/kf/add_contact_way"
                + "?access_token=" + accessToken;

        Map<String, Object> requestBody = new HashMap<>();
        requestBody.put("open_kfid", properties.getOpenKfid());
        requestBody.put("scene", scene);

        WecomContactWayResponse response = restTemplate.postForObject(
                api,
                requestBody,
                WecomContactWayResponse.class
        );

        if (response == null) {
            throw new ServiceException("企业微信客服接口无响应");
        }
        if (response.getErrcode() == null || response.getErrcode() != 0) {
            throw new ServiceException(
                    "获取微信客服链接失败：" + response.getErrmsg()
            );
        }

        String url = validateContactUrl(response.getUrl());
        customerLinkCache.put(cacheKey, url);
        return url;
    }

    private String normalizeScene(String scene) {
        if (scene == null || !ALLOWED_SCENES.contains(scene)) {
            return "guoxin_default";
        }
        return scene;
    }

    private String validateContactUrl(String rawUrl) {
        if (rawUrl == null || rawUrl.isBlank()) {
            throw new ServiceException("企业微信未返回客服链接");
        }

        URI uri = URI.create(rawUrl);
        if (!"https".equalsIgnoreCase(uri.getScheme())
                || !"work.weixin.qq.com".equalsIgnoreCase(uri.getHost())) {
            throw new ServiceException("企业微信返回了不合法的客服链接");
        }
        return rawUrl;
    }
}
```

### 9.3 企业微信响应对象

```java
public class WecomContactWayResponse {

    private Integer errcode;
    private String errmsg;
    private String url;

    public Integer getErrcode() {
        return errcode;
    }

    public void setErrcode(Integer errcode) {
        this.errcode = errcode;
    }

    public String getErrmsg() {
        return errmsg;
    }

    public void setErrmsg(String errmsg) {
        this.errmsg = errmsg;
    }

    public String getUrl() {
        return url;
    }

    public void setUrl(String url) {
        this.url = url;
    }
}
```

## 10. 缓存建议

### access_token 缓存

建议缓存键：

```text
wecom:kf:access_token:{corpId}
```

建议有效期：

```text
7000 秒
```

### 客服链接缓存

建议缓存键：

```text
wecom:kf:contact_url:{openKfid}:{scene}
```

客服链接可按场景长期缓存。当客服帐号、场景配置或企业微信配置发生变化时清除缓存并重新生成。

## 11. scene_param 可选扩展

如果后续需要识别同一场景下的用户或具体入口，可以在企业微信返回的新格式 URL 后追加 `scene_param`。

示例业务参数：

```text
source=quota&userId=123
```

URL 编码后：

```text
source%3Dquota%26userId%3D123
```

最终示例：

```text
https://work.weixin.qq.com/kf/xxxx
?enc_scene=ENCGxxxxxxxx
&scene_param=source%3Dquota%26userId%3D123
```

约束：

- `scene_param` 必须 URL 编码。
- 编码前后均应检查长度，官方限制不超过 128 字节。
- 不建议传递手机号、姓名、身份证号等敏感信息。
- 历史 `encScene` 格式链接不支持 `scene_param`。
- 必须在企业微信返回的完整链接基础上追加，不能复制其他链接的参数。

## 12. 前端配合建议

当前前端只需要继续读取后端返回的 `data.url`：

```ts
const res = await getCustomerServiceLink(scene)
const url = res.data?.url
window.location.href = url
```

前端不负责：

- 获取企业微信 access_token。
- 调用企业微信 `add_contact_way`。
- 生成 `enc_scene`。
- 手工拼接客服帐号地址。

后续如果后端支持 scene 参数，前端应在不同入口传递对应场景：

```text
限时免费：guoxin_free
购买套餐：guoxin_buy
问答不足：guoxin_quota
报告不足：guoxin_report
```

## 13. 联调检查项

后端完成后，依次检查：

1. `/kfLink` 返回状态码为 200。
2. `data.url` 域名为 `work.weixin.qq.com`。
3. 返回 URL 是企业微信接口的完整原始值。
4. URL 包含企业微信生成的场景信息，不能是手工拼接的裸地址。
5. 相同 scene 可以稳定返回有效链接。
6. 不同 scene 可以得到对应的客服入口链接。
7. 企业微信后台可以收到用户进入会话事件。
8. 进入会话事件中的来源场景与请求时的 scene 对应。
9. access_token 过期后可以自动刷新。
10. 企业微信接口异常时，后端返回可理解的业务错误，不泄露敏感配置。

## 14. 验收标准

满足以下条件视为后端改造完成：

- `kfLink` 不再直接返回写死的裸 `kfid` 地址。
- 客服链接由企业微信 `kf/add_contact_way` 接口生成。
- 后端完整保留企业微信返回的 URL 和查询参数。
- 不同业务入口可通过 scene 区分。
- access_token 已缓存，并具备过期刷新机制。
- 前端无需接触企业微信 Secret 和 access_token。
- 企业微信后台能够收到进入会话事件及正确的来源信息。

## 15. 官方参考

- 企业微信“获取客服帐号链接”：<https://open.work.weixin.qq.com/api/doc/90001/90143/94692>
- 企业微信微信客服概述：<https://open.work.weixin.qq.com/api/doc/90001/90143/94657>
